export const setReviews = (state, reviews) => {
  state.reviews = reviews
}

export const setScores = (state, scores) => {
  state.scores = scores
}

export const setAverage = (state, average) => {
  state.average = average
}
